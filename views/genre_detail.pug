extends layout 

block content 
  h1 Genre: #{genre.name} 

  section.mx-auto 
    h3 Books 
    dl 
      each book in genre_books
        dt 
          a(href=book.url)  #{book.title} 
        dd #{book.summary}

      else 
        p No Books of #{genre.name} Genre Found 